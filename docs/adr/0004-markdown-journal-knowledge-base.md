# Markdown Journal Knowledge Base

Kairos uses Markdown files as the durable source of truth for the user's knowledge base, with two built-in artifact categories: diary and record. Attributes such as `tags` live in YAML front matter, while storage location is user-configurable in settings; this keeps the archive human-readable and portable without turning the product model into a directory hierarchy or making a database the only place where user knowledge exists.
